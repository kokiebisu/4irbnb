import { Layout } from '../../../layout/layout.component';
import React from 'react';
import { PrioritySectionProps } from './props';

export const PrioritySection: React.FC<PrioritySectionProps> = () => {
  return (
    <Layout type='section' sectionType='homes'>
      <div>yo</div>
    </Layout>
  );
};
