import { colors } from "./src/colors";
import { root } from "./src/root";
import { styles } from "./src/styles";
import {
  fonts,
  fontWeights,
  lineHeights,
  letterSpacings,
} from "./src/typography";

export const theme = {
  colors,
  root,
  styles,
  fonts,
  fontWeights,
  lineHeights,
  letterSpacings,
  useBodyStyles: false,
};
