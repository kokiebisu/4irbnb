const shared = {
  wrapper: {},
  label: {
    text: {
      color: "white",
    },
  },
};

const web = {
  wrapper: { ...shared.wrapper },
};

const theme = {};

export const border = { web, theme };
