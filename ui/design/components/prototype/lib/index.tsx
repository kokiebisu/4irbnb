import {
  Prototype as CreatePrototype,
  $PROTOTYPE as $CREATE_PROTOTYPE,
} from '@nextbnb/prototype-create/dist/bundle.esm';
import {
  Prototype as GlobePrototype,
  $PROTOTYPE as $GLOBE_PROTOTYPE,
} from '@nextbnb/prototype-globe/dist/bundle.esm';
import {
  Prototype as AuthPrototype,
  $PROTOTYPE as $AUTH_PROTOTYPE,
} from '@nextbnb/prototype-auth/dist/bundle.esm';
import { Prototype as SearchbarPrototype } from '@nextbnb/prototype-searchbar/dist/bundle.esm';
import {
  Prototype as HomesPrototype,
  $PROTOTYPE as $HOMES_PROTOTYPE,
} from '@nextbnb/prototype-homes/dist/bundle.esm';

export {
  CreatePrototype,
  $CREATE_PROTOTYPE,
  GlobePrototype,
  $GLOBE_PROTOTYPE,
  AuthPrototype,
  $AUTH_PROTOTYPE,
  SearchbarPrototype,
  HomesPrototype,
  $HOMES_PROTOTYPE,
};
