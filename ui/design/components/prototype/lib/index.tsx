import {
  Prototype as CreatePrototype,
  $PROTOTYPE as $CREATE_PROTOTYPE,
} from '@nextbnb/prototype-create';
import {
  Prototype as GlobePrototype,
  $PROTOTYPE as $GLOBE_PROTOTYPE,
} from '@nextbnb/prototype-globe';
import {
  Prototype as AuthPrototype,
  $PROTOTYPE as $AUTH_PROTOTYPE,
} from '@nextbnb/prototype-auth';
import { Prototype as SearchbarPrototype } from '@nextbnb/prototype-searchbar';
import {
  Prototype as HomesPrototype,
  $PROTOTYPE as $HOMES_PROTOTYPE,
} from '@nextbnb/prototype-homes';

export {
  CreatePrototype,
  $CREATE_PROTOTYPE,
  GlobePrototype,
  $GLOBE_PROTOTYPE,
  AuthPrototype,
  $AUTH_PROTOTYPE,
  SearchbarPrototype,
  HomesPrototype,
  $HOMES_PROTOTYPE,
};
