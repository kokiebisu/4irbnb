import { colors } from "./colors";
import { root } from "./root";
import { styles } from "./styles";
import { fonts, fontWeights, lineHeights, letterSpacings } from "./typography";

export const theme = {
  colors,
  root,
  styles,
  fonts,
  fontWeights,
  lineHeights,
  letterSpacings,
  useBodyStyles: false,
};
