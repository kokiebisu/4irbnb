import { CustomError } from './custom';
export declare class DatabaseConnectionError extends CustomError {
    statusCode: number;
    constructor();
    serializeErrors: () => {
        message: string;
    }[];
}
